- content_for :admin_content do
  .spacer.md
  .row
    .col-md-8.col-md-offset-2
      %ul.nav.nav-pills
        %li{class: ('active' if controller?('admin/sites'))}
          = link_to "Basic Settings", admin_site_edit_path
        %li{class: ('active' if controller?('admin/page_modules'))}
          = link_to "Page Modules", admin_page_modules_edit_path

      .site-settings-inner
        = yield :site_settings_content

= render template: 'layouts/platform/admin'
